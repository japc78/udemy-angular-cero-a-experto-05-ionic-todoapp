import { FilterFinishedPipe } from './filter-finished.pipe';

describe('FilterFinishedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFinishedPipe();
    expect(pipe).toBeTruthy();
  });
});
